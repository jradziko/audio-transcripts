# Audio Transcription with Speaker Diarization
# Single-file Python tool using Whisper + Pyannote

# Core dependencies
openai-whisper>=20231117
pyannote.audio>=3.1.0

# PyTorch (install version matching your system)
# See README for CUDA-specific installation
torch>=2.1.0
torchaudio>=2.1.0

# Hugging Face integration
transformers>=4.35.0
huggingface-hub>=0.19.0

# Note: tkinter is included with Python on most systems
# Note: FFmpeg must be installed separately (system dependency)
